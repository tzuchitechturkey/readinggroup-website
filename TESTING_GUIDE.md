# 🧪 اختبار نظام التعليقات والردود

## 📋 خطة الاختبار

---

## 1️⃣ اختبار التحميل الأولي

### الاختبار:
```javascript
// في console الفيديو:
console.log("Video ID:", window.location.pathname.split("/").pop());
```

### النتيجة المتوقعة:
✅ يظهر رقم الـ ID

---

## 2️⃣ اختبار تحميل التعليقات

### الخطوات:
1. افتح صفحة الفيديو
2. انتظر 2 ثانية
3. تحقق من ظهور التعليقات

### النتيجة المتوقعة:
✅ تظهر التعليقات من الـ API  
✅ عدد التعليقات = 5 (أو أقل إذا كان هناك تعليقات أقل)  
✅ معلومات المستخدم موجودة  
✅ تاريخ التعليق موجود

### الخطأ المحتمل:
❌ "Failed to load comments"  
💡 **الحل:** تحقق من الـ API endpoint

```javascript
// اختبر هذا في console:
fetch('/comments/?limit=5&offset=0&object_id=1&content_type=video')
  .then(r => r.json())
  .then(d => console.log(d))
```

---

## 3️⃣ اختبار إضافة تعليق

### الخطوات:
1. اكتب تعليق في حقل الإدخال
2. اضغط على زر "Comment"
3. انتظر الرسالة

### النتيجة المتوقعة:
✅ رسالة: "Comment added successfully"  
✅ التعليق الجديد يظهر في الأعلى  
✅ حقل الإدخال يصبح فارغاً

### الخطأ المحتمل:
❌ الزر معطل أو لا يستجيب  
💡 **الحل:** تحقق من:
```javascript
// هل المستخدم مسجل الدخول؟
console.log("Auth Token:", localStorage.getItem('token'));
```

---

## 4️⃣ اختبار الـ Pagination

### الخطوات:
1. انتظر تحميل الصفحة الأولى
2. مرر لأسفل
3. اضغط على "Load More Comments"
4. انتظر التحميل

### النتيجة المتوقعة:
✅ ظهور 5 تعليقات إضافية  
✅ الزر يختفي عند الانتهاء من جميع التعليقات  
✅ لا يوجد تكرار للتعليقات

### اختبار مراقب الشبكة:
```javascript
// افتح DevTools > Network
// اضغط Load More
// يجب أن ترى طلب مع معاملات:
// ?limit=5&offset=5&object_id=1&content_type=video
```

---

## 5️⃣ اختبار الإعجاب

### الخطوات:
1. ابحث عن زر الإعجاب (♥️)
2. اضغط عليه
3. تحقق من التحديث

### النتيجة المتوقعة:
✅ الزر يصبح أزرق/مملوء  
✅ العداد يزيد بـ 1  
✅ رسالة نجاح تظهر

### اختبار إلغاء الإعجاب:
1. اضغط على الزر مرة أخرى
2. الزر يعود لحالته الأصلية
3. العداد ينخفض بـ 1

### الخطأ المحتمل:
❌ الزر لا يستجيب  
💡 **الحل:** تحقق من الـ endpoint:
```bash
curl -X POST http://localhost:8000/comments/1/like/ \
  -H "Authorization: Bearer {token}"
```

---

## 6️⃣ اختبار حذف التعليق

### الخطوات:
1. ابحث عن زر الحذف (🗑️)
2. اضغط عليه
3. تأكد من الحذف

### النتيجة المتوقعة:
✅ التعليق يختفي فوراً  
✅ رسالة: "Comment deleted successfully"  
✅ العدد الكلي يقل بـ 1

### الخطأ المحتمل:
❌ رسالة: "Failed to delete comment"  
💡 **الحول:** قد تكون:
- ليس أنت صاحب التعليق
- الـ API لا تدعم الحذف

---

## 7️⃣ اختبار Emoji Picker

### الخطوات:
1. اضغط على زر Emoji 😊
2. اختر emoji من القائمة
3. تحقق من ظهوره في الحقل

### النتيجة المتوقعة:
✅ Emoji يظهر في حقل الإدخال  
✅ يمكنك إضافة تعليق معه

---

## 8️⃣ اختبار الأخطاء

### اختبار 401 Unauthorized:
```javascript
// احذف الـ token من localStorage:
localStorage.removeItem('token');
// ثم حاول إضافة تعليق
// النتيجة المتوقعة: ❌ "Failed to add comment"
```

### اختبار شبكة معطوبة:
1. افتح DevTools
2. اذهب للـ Network tab
3. اختر "Offline"
4. حاول إضافة تعليق
5. النتيجة المتوقعة: ❌ "Failed to add comment"

### اختبار Server Error:
```javascript
// إذا كان الـ server معطوب:
// النتيجة المتوقعة: ❌ "Failed to add comment"
```

---

## 9️⃣ اختبار المقالات

### نفس الخطوات السابقة لكن:
```jsx
// استخدم PostCommentsSection بدلاً من CommentsSection
<PostCommentsSection postId={postId} />
```

### الاختلافات:
- content_type = "post" بدلاً من "video"
- نفس الميزات والـ functionality

---

## 🔟 اختبار Performance

### اختبار 1000 تعليق:
```javascript
// اختبر في console:
const startTime = performance.now();
await GetVideoComments(videoId, 1000, 0);
console.log("Time:", performance.now() - startTime, "ms");
```

### النتيجة المتوقعة:
✅ الوقت أقل من 1 ثانية  
✅ الواجهة تبقى سريعة

---

## 📊 قائمة الاختبارات

| رقم | الاختبار | الحالة | الملاحظات |
|-----|---------|--------|----------|
| 1 | تحميل التعليقات | ❓ | |
| 2 | إضافة تعليق | ❓ | |
| 3 | الـ Pagination | ❓ | |
| 4 | الإعجاب | ❓ | |
| 5 | حذف التعليق | ❓ | |
| 6 | Emoji Picker | ❓ | |
| 7 | معالجة الأخطاء | ❓ | |
| 8 | الأداء | ❓ | |
| 9 | المقالات | ❓ | |
| 10 | الردود | ❓ | |

---

## 🎯 مدخلات الاختبار

### بيانات صحيحة:
```javascript
// تعليق صحيح
const comment = "هذا تعليق رائع جداً! 😊";

// معرف صحيح
const videoId = 123;

// معرف غير صحيح
const invalidId = "abc";
```

---

## 🔄 سيناريوهات الاختبار

### السيناريو 1: مستخدم جديد
1. افتح الموقع
2. اذهب لصفحة الفيديو
3. شاهد التعليقات
4. تسجيل الدخول
5. أضف تعليق

### السيناريو 2: مستخدم متقدم
1. سجل دخول
2. أضف تعليق
3. أعجب بتعليق
4. اضغط Load More
5. احذف التعليق

### السيناريو 3: عطل الشبكة
1. شغل Offline Mode
2. حاول إضافة تعليق
3. تحقق من الرسالة
4. أعد الاتصال
5. حاول مرة أخرى

---

## 📝 ملاحظات للاختبار

### ما يجب اختباره بالتفصيل:
- ✅ جميع الأزرار تعمل
- ✅ جميع الرسائل تظهر
- ✅ البيانات صحيحة
- ✅ الـ UI سريع
- ✅ لا توجد أخطاء في console

### ما لا يجب أن يحدث:
- ❌ أخطاء في console
- ❌ تجميد الواجهة
- ❌ فقدان البيانات
- ❌ تكرار غير متوقع

---

## 🛠️ أدوات الاختبار المقترحة

### 1. Developer Tools:
```javascript
// اختبر API endpoints مباشرة
fetch('/comments/?limit=5&offset=0&object_id=1&content_type=video')
  .then(r => r.json())
  .then(d => console.table(d.results))
```

### 2. Postman:
- استورد API collection
- اختبر جميع الـ endpoints

### 3. Browser DevTools:
- Network tab: تابع الطلبات
- Console: تتبع الأخطاء
- Performance: قياس السرعة

---

## 📋 تقرير الاختبار

### التاريخ: [اليوم]
### الاختبر: [اسمك]

| الاختبار | النتيجة | التفاصيل |
|---------|---------|----------|
| | ✅/❌ | |

---

## ✨ الخلاصة

قائمة الاختبار جاهزة! جرب كل اختبار وقم بتوثيق النتائج.

إذا حدث أي خطأ، راجع القسم المناسب في التوثيق.

**استمتع بالاختبار! 🚀**

